upstream backend {
    server backend:8000;
}

server {
    listen 80;
    server_name _;

    client_max_body_size 20M;

    location /static/ {
        alias /app/staticfiles/;
    }

    location /media/ {
        alias /app/media/;
    }

    location / {
        proxy_pass http://backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Uncomment once Certbot is added (see docker-compose.yml comments)
    # so it can serve the ACME HTTP-01 challenge over plain :80:
    # location /.well-known/acme-challenge/ {
    #     root /var/www/certbot;
    # }
}

# ── Adding HTTPS later (Certbot/Let's Encrypt) ──────────────────────
# 1. Add a `certbot` service to docker-compose.yml and the
#    certbot_www / certbot_conf volumes (already stubbed there).
# 2. Uncomment the acme-challenge location above and mount certbot_www
#    here.
# 3. Add a second server block:
#      server {
#          listen 443 ssl;
#          server_name your-domain.com;
#          ssl_certificate     /etc/letsencrypt/live/your-domain.com/fullchain.pem;
#          ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
#          # ... same location blocks as above
#      }
# 4. Change `listen 80` above to redirect to https:// once certs exist.
